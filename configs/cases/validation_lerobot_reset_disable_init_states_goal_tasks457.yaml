schema_version: "1.0"
kind: case
case:
  case_id: validation_lerobot_reset_disable_init_states_goal_tasks457
  platform: lerobot_libero
  case_family: setup_sensitive_factor
  deviation_symptom: evaluation_instability_or_flakiness
  expected_status: likely_setup_sensitive_deviation
  expected_factor: reset_or_initial_state
  injected_factor: reset_or_initial_state
  injection_operator: reset.disable_fixed_init_state
  rq1_factor_category: reset_or_initial_state
  rq1_evidence_refs:
    - github_issue::huggingface/lerobot::3814
  rq1_support_level: evidence_backed
  coverage_status: core_planned
  artifact_split: validation
baseline_runs:
  - platform: lerobot_libero
    run_id: validation_lr_goal_tasks457_ep1_baseline_seed1000_reset_v2
    role: baseline
    suite: libero_goal
    task_ids: [4, 5, 7]
    seed: 1000
    episodes: 1
    output_root: /data/project/zjx/runs/evaltriage
    policy_path: /data/project/zjx/checkpoints/lerobot/pi0_libero_finetuned_v044
    obs_type: pixels_agent_pos
    camera_size: 360
    libero_init_states: true
    compile_model: false
    use_async_envs: false
current_runs:
  - platform: lerobot_libero
    run_id: validation_lr_goal_tasks457_ep1_current_init_states_false_seed1000
    role: current
    suite: libero_goal
    task_ids: [4, 5, 7]
    seed: 1000
    episodes: 1
    output_root: /data/project/zjx/runs/evaltriage
    policy_path: /data/project/zjx/checkpoints/lerobot/pi0_libero_finetuned_v044
    obs_type: pixels_agent_pos
    camera_size: 360
    libero_init_states: false
    compile_model: false
    use_async_envs: false
    injection:
      enabled: true
      factor: reset_or_initial_state
      operator: reset.disable_fixed_init_state
      params:
        init_states: false
replay_runs:
  - platform: lerobot_libero
    run_id: validation_lr_goal_tasks457_ep1_replay_seed1000_reset_v2
    role: replay
    suite: libero_goal
    task_ids: [4, 5, 7]
    seed: 1000
    episodes: 1
    output_root: /data/project/zjx/runs/evaltriage
    policy_path: /data/project/zjx/checkpoints/lerobot/pi0_libero_finetuned_v044
    obs_type: pixels_agent_pos
    camera_size: 360
    libero_init_states: true
    compile_model: false
    use_async_envs: false
thresholds_path: configs/thresholds/validation_lerobot_libero.yaml
