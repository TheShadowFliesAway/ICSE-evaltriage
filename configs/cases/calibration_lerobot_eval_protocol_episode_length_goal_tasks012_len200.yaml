schema_version: "1.0"
kind: case
case:
  case_id: calibration_lerobot_eval_protocol_episode_length_goal_tasks012_len200
  platform: lerobot_libero
  case_family: setup_sensitive_factor
  deviation_symptom: success_rate_drop_or_mismatch
  expected_status: likely_setup_sensitive_deviation
  expected_factor: evaluation_protocol_metric
  injected_factor: evaluation_protocol_metric
  injection_operator: eval_protocol.change_episode_length
  rq1_factor_category: evaluation_protocol_metric
  rq1_evidence_refs:
    - github_issue::huggingface/lerobot::1316
  rq1_support_level: evidence_backed
  coverage_status: core_planned
  artifact_split: validation
baseline_run_ids:
  - calibration_lr_goal_tasks012_ep1_baseline_seed1000
current_runs:
  - platform: lerobot_libero
    run_id: calibration_lr_goal_tasks012_ep1_current_episode_length_200_seed1000
    role: current
    suite: libero_goal
    task_ids: [0, 1, 2]
    seed: 1000
    episodes: 1
    output_root: /data/project/zjx/runs/evaltriage
    policy_path: /data/project/zjx/checkpoints/lerobot/pi0_libero_finetuned_v044
    obs_type: pixels_agent_pos
    camera_size: 360
    episode_length: 200
    compile_model: false
    use_async_envs: false
    injection:
      enabled: true
      factor: evaluation_protocol_metric
      operator: eval_protocol.change_episode_length
      params:
        episode_length: 200
replay_run_ids:
  - calibration_lr_goal_tasks012_ep1_replay_seed1000
thresholds_path: configs/thresholds/validation_lerobot_libero.yaml
